# Instructions:
# Go the bottom of the file and edit the inputs for what you need done
#--------------------------------------------------------------------
def midpoint(line_points):
    point_one = (line_points[0] + line_points[3]) / 2
    point_two = (line_points[1] + line_points[2]) / 2
    print(point_one,point_two)

#Example:
midpoint([-8,1,2,1])
